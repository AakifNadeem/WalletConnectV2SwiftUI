import XCTest
@testable import WalletConnectV2SwiftUI

final class WalletConnectV2SwiftUITests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WalletConnectV2SwiftUI().text, "Hello, World!")
    }
}
